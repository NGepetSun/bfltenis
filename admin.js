const $=s=>document.querySelector(s);let state=null;
function name(p){return typeof p==="string"?p:(p?.name||"TBD")}
async function api(action,body){const r=await fetch("/api/index?action="+action,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body||{})});const d=await r.json();if(!r.ok)throw Error(d.error||"Request gagal");return d}
function drawConnectors(container){const svg=container.querySelector("svg.bracket-lines"),bracketEl=container.querySelector(":scope > .bracket");if(!svg||!bracketEl)return;const w=bracketEl.offsetWidth,h=bracketEl.offsetHeight;svg.setAttribute("width",w);svg.setAttribute("height",h);svg.setAttribute("viewBox",`0 0 ${w} ${h}`);const refRect=bracketEl.getBoundingClientRect();const rounds=[...bracketEl.querySelectorAll(":scope > .round:not(.third-place)")];let html="";rounds.forEach((roundEl,ri)=>{if(ri===rounds.length-1)return;const nextMatches=rounds[ri+1].querySelectorAll(".match"),matches=roundEl.querySelectorAll(".match");matches.forEach((m,mi)=>{const t=nextMatches[Math.floor(mi/2)];if(!t)return;const r1=m.getBoundingClientRect(),r2=t.getBoundingClientRect();const x1=r1.right-refRect.left,y1=r1.top+r1.height/2-refRect.top,x2=r2.left-refRect.left,y2=r2.top+r2.height/2-refRect.top,midX=x1+(x2-x1)/2;const won=m.querySelector(".win");html+=`<path d="M ${x1} ${y1} H ${midX} V ${y2} H ${x2}" fill="none" stroke="${won?"rgba(255,216,117,.6)":"rgba(207,118,255,.4)"}" stroke-width="2"/>`});});svg.innerHTML=html}
function bracket(s){const el=$("#adminBracket");if(!s.rounds?.length){el.innerHTML='<div class="empty">Bracket belum dimulai.</div>';return}const roundsHtml=s.rounds.map(r=>`<div class="round"><h3>${r.name}</h3><div class="matches">${r.matches.map(m=>`<div class="match ${m.status==="live"?"live":""}">${m.players.map(p=>{const n=name(p);return `<div class="slot ${m.winner&&name(m.winner)===n?"win":m.winner?"lose":""}" ${m.status==="live"&&n!=="TBD"?`data-mid="${m.id}" data-name="${encodeURIComponent(n)}"`:""}>${n}<span class="tag">${m.status==="live"?"KLIK UNTUK MENANGKAN":m.winner&&name(m.winner)===n?"WIN":""}</span></div>`}).join("")}</div>`).join("")}</div></div>`).join("");const thirdHtml=s.thirdPlace?`<div class="round third-place"><h3>3RD PLACE</h3><div class="matches"><div class="match ${s.thirdPlace.status==="live"?"live":""}">${s.thirdPlace.players.map(p=>{const n=name(p);return `<div class="slot" ${s.thirdPlace.status==="live"&&n!=="TBD"?`data-mid="third" data-name="${encodeURIComponent(n)}"`:""}>${n}</div>`}).join("")}</div></div></div>`:"";el.innerHTML=`<svg class="bracket-lines"></svg><div class="bracket admin-bracket">${roundsHtml}${thirdHtml}</div>`;el.querySelectorAll("[data-mid]").forEach(x=>x.onclick=()=>winner(x.dataset.mid,decodeURIComponent(x.dataset.name)));requestAnimationFrame(()=>drawConnectors(el))}
window.addEventListener("resize",()=>{const c=$("#adminBracket");if(c)drawConnectors(c)});
async function load(){try{const r=await fetch("/api/index?action=state",{cache:"no-store"});state=await r.json();if(state.admin){$("#loginBox").hidden=true;$("#panel").hidden=false;renderPlayers();bracket(state)}else{$("#loginBox").hidden=false;$("#panel").hidden=true}}catch(e){$("#loginMsg").textContent=e.message}}
function renderPlayers(){const p=state.players||[];$("#count").textContent=p.length+" PLAYER";$("#players").innerHTML=p.map((x,i)=>`<div class="player-row"><input data-n="${i}" class="pn" value="${esc(name(x))}"><input data-y="${i}" class="py" value="${esc(x?.youtube||"")}" placeholder="YouTube URL"><button data-save="${i}">SAVE</button></div>`).join("");document.querySelectorAll("[data-save]").forEach(b=>b.onclick=async()=>{const i=b.dataset.save;try{await api("update-player",{index:+i,name:$(`.pn[data-n="${i}"]`).value,youtube:$(`.py[data-y="${i}"]`).value});$("#adminMsg").textContent="Player tersimpan.";load()}catch(e){$("#adminMsg").textContent=e.message}})}
function esc(x){return String(x).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll('"',"&quot;")}
async function winner(mid,n){if(!confirm("Menangkan "+n+"?"))return;try{await api("winner",{matchId:mid,playerName:n});$("#adminMsg").textContent=n+" menang dan otomatis maju.";load()}catch(e){$("#adminMsg").textContent=e.message}}
$("#loginForm").onsubmit=async e=>{e.preventDefault();try{await api("login",{username:$("#username").value,password:$("#password").value});load()}catch(x){$("#loginMsg").textContent=x.message}}
$("#addForm").onsubmit=async e=>{e.preventDefault();try{await api("add-player",{name:$("#playerName").value,youtube:$("#playerYoutube").value});$("#playerName").value="";$("#playerYoutube").value="";$("#adminMsg").textContent="Player ditambahkan.";load()}catch(x){$("#adminMsg").textContent=x.message}}
$("#startBtn").onclick=async()=>{if(!confirm("Mulai/randomize bracket sekarang?"))return;try{await api("start");$("#adminMsg").textContent="Bracket dimulai.";load()}catch(e){$("#adminMsg").textContent=e.message}}
$("#resetBtn").onclick=async()=>{if(!confirm("RESET SEMUA DATA TURNAMEN?"))return;try{await api("reset");$("#adminMsg").textContent="Tournament di-reset.";load()}catch(e){$("#adminMsg").textContent=e.message}}
$("#logoutBtn").onclick=async()=>{await api("logout");location.reload()}
load();setInterval(()=>{if(state)load()},1500);
